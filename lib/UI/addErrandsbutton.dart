/*import 'package:flutter/material.dart';

class addErrandsbutton extends StatefulWidget {
  const addErrandsbutton({ Key? key }) : super(key: key);

  @override
  State<addErrandsbutton> createState() => _addErrandsbuttonState();
}

class _addErrandsbuttonState extends State<addErrandsbutton> {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Container(
            floatingActionButton: FloatingActionButton.extended(
          onPressed: () {
            Navigator.push (
            context,
            MaterialPageRoute (builder: (context)=> submitErrand()),
            );
            }, //addErrand
          label: const Text('Add Errand'),
          icon: const Icon(Icons.add),
          backgroundColor: Colors.pink,
        ),
        ),
      ),
      
      
    );
  }
} */